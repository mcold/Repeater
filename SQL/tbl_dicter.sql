create table if not exists word(id 			integer primary key autoincrement,
								lang 		text,
								name 		text,
								ru			text,
								genus		text,
								plur_end 	text,
								url 		text,
								view_date	date);
									
